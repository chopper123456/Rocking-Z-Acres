import CornProjectionTool from './CornProjectionTool'

export default function CornProjection() {
  return <CornProjectionTool />
}
```

Save it, then run:
```
git add .
git commit -m "fix corn projection wrapper"
git push
